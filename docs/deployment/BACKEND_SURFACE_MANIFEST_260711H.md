# Backend-surface manifest (260711-H Phase 2c prep) — the exact fresh-copy list for the x-backend seed

At the operator's freeze SHA, x-backend is seeded by fresh-copying EXACTLY these paths (no git history;
provenance is `git ls-tree -r <SHA>` blob hashes in x-backend/MIGRATION-PROVENANCE.md). Verified against
the 260711-H split survey; everything here is backend and deploys independently of the frontend.

## COPY (backend runtime + tests + config)
- `src/workers/**` — 429 files, ~59k LoC (routes 40 · dal 75 · lib 21 · services 18 · crons 12 ·
  middleware 7 · db/migrations 66 · __tests__ 148 · index.ts + sentry.ts + telemetry.ts + sources/
  inference/ graph/ inference/).
- `functions/**` — 16 Cloudflare Pages Functions (the second edge-backend: api/{feedback,session,
  csp-report,receipts,proposals}, api/paid-pilot, api/actions/*, api/health, api/telemetry, _lib/,
  _middleware.js). NOTE: these deploy WITH the Pages project today; Phase 4b routes /api/* through the
  worker before freeze, after which this dir's deploy obligation moves to the worker.
- `migrations/**` — 4 top-level D1/Pages SQL (feedback annotations · customer-feedback + paid-pilot
  authority · investor portal).
- `packages/xlooop-mcp-server/**` — the MCP server (src/tests/docs), backend-adjacent.
- `wrangler.toml` · `wrangler.jsonc` · `vitest.workers.config.ts` · `tsconfig.workers.json`
  (standalone, no extends — the base `tsconfig.json` is frontend-only and NOT copied) · `.nvmrc`/node
  version pin if present. (260711-I: `wrangler.test.generated.toml` DROPPED from the copy list — it is
  inline-generated by vitest.workers.config.ts at every run and gitignored; only wrangler.toml must
  sit beside the vitest config.)
- Backend gate scripts: the class-A `scripts/verify-*.mjs` from GATE_DISPOSITION_LEDGER_260711H.md +
  their `scripts/lib/` helpers + `scripts/seam-gates/` + `scripts/git-hooks/` + `scripts/emit-deploy-receipt.mjs`
  + `scripts/verify-deploy-sha-current.mjs` + `scripts/repo-schema-gen.mjs` (regenerate fresh) +
  `scripts/secret-scan.mjs`. A FORKED `scripts/ci-local.mjs` (class-A gate list only + the 6 boundary gates).
- `data/` backend subset: `data/schemas/*` (3 JSON Schemas) · `spine-authority-vocabulary.v1.json` ·
  `mbp-*` projection/receipt/manifest JSONs · `operations-live-stream.json` ·
  **`document-context-read-model.json`** (REHEARSAL GAP #2 — the third verify:data-schemas binding;
  was missing from this list) · `me.json` · `nav-config.json` · `spaces.json` · `ws-*.json` ·
  `xcp-shared-access-contract-pack.v1.json` · `_tenant-manifests/` · `projects/` · `fixtures/` ·
  `orphan-test-baseline.json` **PRUNED of the 3 dropped facade-test entries at copy time**
  (REHEARSAL GAP #3 — the shrink-only ratchet goes RED on stale entries otherwise).
  260711-I C2 UPDATE: after burn-down batch 1 the baseline holds ONLY those 3 facade entries
  (max_orphans 3) — pruning them at seed time empties `exempt` and sets max_orphans to 0; the
  97 burned-down tests ride into the seed via the 4 class-A verify:worker-unit-suite-* gates
  in the forked ci-local.
- Backend docs: `docs/deployment/**` · `docs/security/**` · `docs/governance/**` · `docs/contracts/**` ·
  `docs/adrs/**` · `docs/standards/**` · `docs/architecture/backend/**` · `docs/graph/**` (gate inputs) ·
  the frontend-migration ledgers/manifests that are gate inputs (backend-ui-contract-ledger, reconciliation
  SSOT, DISABLED_CONTRACT_SPECS, IDEMPOTENCY_MECHANISM, this manifest + the gate-disposition ledger).
- `package.json` TRIMMED to: 6 runtime deps (hono · @neondatabase/serverless · @clerk/backend ·
  @sentry/cloudflare · nanoid · unpdf) + backend devDeps (vitest · @cloudflare/vitest-pool-workers ·
  wrangler · typescript · **@cloudflare/workers-types** [REQUIRED — tsconfig.workers.json `types`
  references it; typecheck fails without it] · **@babel/parser** [repo-schema-gen.mjs imports it —
  kept because fresh-schema-per-repo is the convention]). 260711-I corrections: tsx is NOT a dep of
  this repo (removed from the list); esbuild is class-D-only (dropped). DROP: react/react-dom ·
  storybook/* · vite/* · @playwright/test · @axe-core/playwright · vite-plugin-singlefile ·
  @xcp/primitives · @xcp/tokens (file: links to xcp-platform).
  **LOCKFILE**: `npm ci` requires a lockfile in sync with the trimmed package.json — the REHEARSAL
  uses `npm install`; the REAL SEED's deliverables include `npm install --package-lock-only` to
  regenerate and commit the lockfile.
  **EXACT PINS (REHEARSAL GAP #1 — caret ranges ERESOLVE)**: wrangler ^4.93.1 floats to 4.110+
  which peer-requires workers-types ^5.x; the seed package.json pins the monorepo's lockfile-resolved
  versions exactly: wrangler 4.93.1 · typescript 5.9.3 · vitest 2.1.9 · vitest-pool-workers 0.5.41 ·
  workers-types 4.20260606.1 · @babel/parser 7.29.2 · hono 4.12.23 · @neondatabase/serverless 0.10.4 ·
  @clerk/backend 1.34.0 · @sentry/cloudflare 10.63.0 · nanoid 5.1.11 · unpdf 1.6.2.
  **KEEP the 2 RLS gate scripts (REHEARSAL GAP #4)**: the trimmed scripts block MUST retain
  `verify:operational-spine-api` + `verify:operational-spine-live-rls` — dropping them silently
  de-gates 2 backend suites (the orphan gate resolves gating from package.json script keys too).
- **DROP from the copy (260711-I)**: `src/workers/__tests__/{session-facade,api-facade,iam-parity}.test.ts`
  — they import `src/shared/platform/*` (frontend facades); they are baseline-exempt orphans that
  would break any vitest sweep in the seeded tree. Their C2 triage verdict = move-to-x-front or
  delete-candidate, never gate-now in x-backend.
- `.gitignore` · `CLAUDE.md` (new — backend routing rules, x-front template) · `README.md` (new).
- `MIGRATION-PROVENANCE.md` (new — seed SHA + ls-tree blob hashes for mechanical parity).

## DO NOT COPY (frontend-superseded — stays in the frozen XCP-demo as history)
`src/widgets` · `src/shared` · `src/runtime` · `src/app` · `src/pages` · `src/components` · `src/entities` ·
`src/entry` · `src/investor` · `src/contracts` (except what the snapshot-pin gate fixtures capture) ·
`src/features`/`src/ui`/`src/theme` · `dist/**` · `dist-tenant-*/**` · `index.html` · `index.standalone.html` ·
`.storybook/` + `*.stories.*` · `vite.config.ts` · `vite.standalone.config.ts` · `playwright*.config.ts` ·
`design-handoff/` · `prototypes/` · `legacy/` · the frontend `docs/` subtrees (frontend/ · frontend-migration/
program docs (non-gate) · sessions/ · disposition/ · handoffs/ · audits/ · _archive/) — those follow the
x-ai-docs disposition ledger or stay frozen.

## Independent-proof checklist (Phase 2c, after copy — 260711-I corrected)
0. `git init` + commit the seed tree FIRST (verify:production-readiness-state freshness is git-aware;
   an uncommitted copy falls back to mtime comparison and is copy-order-flaky).
1. `npm ci` with the regenerated lockfile (rehearsal: `npm install`) — proves the file: links dropped.
2. `npm run typecheck:workers` clean (needs @cloudflare/workers-types).
3. Forked `ci-local` (CORRECTED class-A list + boundary gates) green, WITH the lockstep-forked
   `GOVERNANCE_PILLARS.yml` (frontend gate ids stripped) in the same commit.
4. `npm run deploy:api:dryrun` OK — the repo's existing proven-offline command (`wrangler deploy
   --dry-run --outdir dist-workers-dryrun`; auth-free, verified in wrangler 4.93.1; leaves an
   inspectable bundle). **This is a BUNDLE-VALIDITY proof, NOT a deployability proof** — account-side
   limits (the 5-cron cap, error 10072 in this repo's own history; custom-domain routes; [ai]
   binding; secrets) surface only at real deploy.
5. `git ls-tree -r HEAD -- <copied paths>` hash-diff == MIGRATION-PROVENANCE.md.
6. `scripts/git-hooks/install-hooks.sh` run (local pre-push IS the CI — no remote CI by policy).
7. Known machine-couplings recorded: identity-contracts (xcp-platform abs path) · merged-ledger
   freshness (x-ai-front abs path) — same-machine OK, flagged for any future CI host.
